type CheckBox = {
  id: number;
  name: string;
  label: string;
  isChecked: boolean;
  disabled: boolean;
};

type Some = {
  uppercase: boolean;
  lowercase: boolean;
  symbols: boolean;
  numbers: boolean;
};
