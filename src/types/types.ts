type CheckBox = {
  id: number;
  name: string;
  label: string;
  isChecked: boolean;
  disabled: boolean;
};

type PasswordProps = {
  uppercase: boolean;
  lowercase: boolean;
  symbols: boolean;
  numbers: boolean;
};

// type X = {
//   [key: string]: boolean;
// };
